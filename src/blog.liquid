---
layout: default
title: "Blog da Cruz"
---

<div class="container my-5">
  <nav aria-label="breadcrumb">
    <ol class="breadcrumb">
      <li class="breadcrumb-item">
        <a href="/"><i class="fas fa-arrow-left me-1"></i>Home</a>
      </li>
    </ol>
  </nav>
</div>

<div class="container mt-5 pt-5">
  <div class="blog">
    <h2>Blog da Cruz</h2>
    <p>
      Um espaço dedicado para notícias, reflexões e mensagens compartilhadas por leigos, padres e toda a comunidade paroquial.
    </p>
  </div>

  <div class="row">
    {% assign posts_ordenados = site.data.blog | sort: "data" | reverse %}
    {% for post in posts_ordenados %}
    <div class="col-md-12 mb-4">
      <div class="card h-100 shadow-sm">
        <img src="{{ post.imagem }}" class="card-img-top" alt="Imagem da postagem '{{ post.titulo }}'">
        <div class="card-body d-flex flex-column">
          <h5 class="card-title">{{ post.titulo }}</h5>
          <p class="card-text text-muted"><i class="far fa-user me-1"></i>{{ post.autor }}</p>
          <p class="card-text text-muted"><i class="far fa-calendar-alt me-1"></i>{{ post.data | date: "%d/%m/%Y" }}</p>
          <p class="card-text">{{ post.resumo }}</p>
          <button class="btn btn-outline-dark mt-auto" data-bs-toggle="modal" data-bs-target="#modalPost{{ forloop.index }}">
            LER MAIS
          </button>
        </div>
      </div>
    </div>

    <!-- Modal -->
    <div class="modal fade" id="modalPost{{ forloop.index }}" tabindex="-1" aria-labelledby="modalLabel{{ forloop.index }}" aria-hidden="true" role="dialog">
      <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="modalLabel{{ forloop.index }}">{{ post.titulo }}</h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fechar"></button>
          </div>
          <div class="modal-body">
            <p><strong>Autor:</strong> {{ post.autor }}</p>
            <p><strong>Data:</strong> {{ post.data | date: "%d/%m/%Y" }}</p>
            <hr>
            <p>{{ post.conteudo | newline_to_br }}</p>
          </div>
        </div>
      </div>
    </div>
    {% endfor %}
  </div>
</div>
